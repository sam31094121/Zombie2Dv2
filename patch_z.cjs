const fs = require("fs"); let c = fs.readFileSync("src/game/Zombie.ts", "utf8"); c = c.replace(/constructor\(x: number, y: number, type: ZombieType = \x27normal\x27\) {[\s\S]*?this\.speed  = def\.baseSpeed;\s*}/, `constructor(x: number, y: number, type: ZombieType = \x27normal\x27) { this.init(x, y, type); } init(x: number, y: number, type: ZombieType = \x27normal\x27) { this.x = x; this.y = y; this.type = type; const def = ZOMBIE_REGISTRY[type]; this.radius = def.radius; this.hp = def.baseHp; this.maxHp = def.baseHp; this.speed = def.baseSpeed; this.vx = 0; this.vy = 0; this.paralysisTimer = 0; this.slowTimer = 0; this.flashWhiteTimer = 0; this.isInfiniteGlow = false; this.leanBackTimer = 0; this.isInsideContainer = false; this.time = 0; this.isCloseToPlayer = false; this.angle = 0; this.isSummoned = false; this.jellyPhase = 0; this.lastSpitTime = 0; this.lastTrailTime = 0; this.extraState.clear(); this.lastDamageTime.clear(); }`); fs.writeFileSync("src/game/Zombie.ts", c);
