Original prompt: 瘝??????
- 2026-03-30: 瑼Ｘ? Lv1 ?單??亦?憭望????璅蝣箄? slash_01.wav.mp3 ?臬撖阡?鋡怨??亥??剜??
- 2026-03-30: ?潛? Lv1~4 銝餉?韏?WeaponDefinitions.ts ??fireDirect()嚗??臬?靘???CombatSystem slash ???
- 2026-03-30: 撠? Lv1 ?單??剜敺?HTMLAudio ?寧 AudioContext + decodeAudioData嚗蒂??init() ??頛?/audio/weapons/sword/slash_01.wav.mp3??
- 2026-04-15: Arena 過場改為戰利品袋流程。波次結束後不再直接自動吸到玩家，而是丟出袋子，只吸地上原本存在的 `energy_orb`。
- 2026-04-15: 過場中剩餘怪物直接爆死，但不掉 orb，也不掉一般道具；地上的非 orb 道具會在封袋開始時直接爆掉清掉。
- 2026-04-15: 下一波會延遲生成袋子怪（用 `normal` 殭屍外觀加 bagCarrier 標記實作），打死後掉回上一波封袋值的 80% 金色 orb；如果拖到清場才死，不返還。
- 2026-04-15: UI 補了兩個提示：`戰利品封袋中` 與 `袋子怪攜帶 X`。
- 2026-04-15: 補修 `TestModePanel.tsx` 的 `ghost` 半徑對照，讓 `npm run lint` 可通過。
- 2026-04-15: 驗證結果：`npm run lint`、`npm run build` 通過。Playwright client 需要額外安裝 `playwright` 與 Chromium，已完成；自動點擊可成功進入遊戲並產生截圖於 `output/web-game*`。
- 2026-04-15: 目前限制：專案還沒有針對競技場過場的自動化 hook / 狀態輸出，這次只驗證了頁面載入、開始遊戲、無新增 console error，尚未自動走到「封袋 -> 商店 -> 下一波袋子怪」完整鏈路。
- 2026-04-20: Arena pacing patch landed. Director now counts queued `spawn_warning` weight and ramps toward target pressure over the opening seconds instead of snapping to full wave pressure.
- 2026-04-20: Arena wave tombstones are tagged and removed from `mapManager.obstacles` when entering shop / starting the next wave, preventing cross-wave tombstone carryover.
- 2026-04-20: Wave HUD now receives objective text so objective waves show mission text rather than exposing the raw `999` timer.
- 2026-04-21: Endless-mode hotfix in progress. Restored timer-based spawn from `Game.update()` for non-arena modes after arena-only `DirectorAI` cut off the old endless spawn path.
- 2026-04-21: Arena director spawn anchors now choose random safe points inside the arena instead of hugging the map edge, relying on `spawn_warning` telegraphs to keep the spawn readable.
- 2026-04-21: Arena pressure curve now follows wave stage milestones: 20% at wave start, 50% at the half-way mark, and full pressure by the start of the final third (objective waves use a virtual 30s timeline).
- 2026-04-21: Gun Lv1 and Lv2 bullets now pierce 2 zombies, giving the early gun path a small clear-speed bump without touching later gun branches.
- 2026-04-21: Enemy projectiles no longer damage tombstones, preventing cross tombstones from self-destructing to monster spit.
- 2026-04-21: `npm run lint` and `npm run build` both passed after the pacing / spawn / weapon tweaks; local smoke scripts also launched the game successfully for a quick regression check.
- 2026-04-22: Slime pressure was softened by reducing slime trail density and lifetime, shortening trail-applied slow to a short refresh window, and lowering both slime and small-slime chase speed so players can actually break out after being tagged.
- 2026-04-22: Fire altar now lets sword hits proc the Lv6 sword lava/explosion module. Base/A swords consume one altar proc per thrown blade; branch B swords are upgraded to at least Lv6 lava stats while standing in the altar.
- 2026-04-22: Monolith energy turrets now charge from player bullets faster, fire larger blue homing missiles, and use blue/arc hit feedback so the payoff reads clearly on screen.
- 2026-04-22: Vending machine HP dropped to 18. Destroyed vending wrecks now fade out after a short delay and render in the floor layer so they no longer cover characters or drops.
- 2026-04-22: Explosive barrel ground shadow/outline was re-centered and softened to remove the skewed black ring look.
- 2026-04-22: `npm run lint` and `npm run build` passed after these changes. Local Vite + Playwright smoke verified the menu still renders (`output/web-game/shot-1.png`), but the quick scripted click did not advance past the main menu, so the new monolith / altar visuals still need an in-game targeted smoke pass.
- 2026-04-22: Monolith was overhauled toward a true energy missile cannon. It now queues a 5-shot volley, fires each missile from the barrel in cadence, gives missiles an obstacle grace period plus delayed homing so they visibly leave the turret before turning, and upgrades the obstacle render into a rotating cannon/turret silhouette instead of a plain crystal obelisk.
- 2026-04-22: Energy missiles were enlarged and given heavier tails/core proportions. Current balance pass uses 11 direct damage, single-hit impact, and a small splash radius on detonation to help the volley read as "heavy missiles" rather than light blue darts.
- 2026-04-22: `npm run lint` and `npm run build` still pass after the monolith cannon rework. Boot smoke via local Vite + Playwright succeeded and captured the menu in `output/monolith-menu-smoke/shot-1.png`, but monolith-specific in-game smoke coverage is still TODO.
- 2026-04-22: Monolith charge intake now supports sword damage too. Both `slash` projectiles and thrown/returning sword hits route through the same `Game.chargeMonolith(...)` helper, and slash attacks track obstacle hit keys so one swing does not multi-charge the tower every frame.
- 2026-04-23: Mobile camera view was nudged closer by shrinking the mobile-only logical short side from `800` to `720` in `GameUI.tsx`. This is about an `11.1%` zoom-in on mobile (`800 / 720 = 1.111...`) while leaving desktop framing unchanged.
- 2026-04-23: Spitter AI now treats blocked line-of-sight as a reposition state. In `ZombieDefinitions.ts`, spitters keep a short-lived strafe vector in `extraState`, shuffle laterally with a light range bias while blocked, then require a short re-aim window before firing once LOS is restored.
- 2026-04-23: `npm run lint` and `npm run build` both passed after the spitter AI change.
- 2026-04-23: Playwright smoke is still pending. Sandbox launch of `npm run dev` failed with `vite/esbuild spawn EPERM`, so an escalated dev-server launch was requested in order to complete the required browser smoke test.
- 2026-04-23: Added `output/static-server.cjs` as a tiny local static server helper for build smoke runs, because the sandboxed Vite dev server and Playwright browser launch were both hitting `spawn EPERM`.
- 2026-04-23: Escalated Playwright smoke against the built app succeeded with a clean fresh output folder at `output/spitter-smoke-clean/shot-0.png`. No fresh `errors-0.json` was emitted there, so this run did not surface a new console error.
- 2026-04-23: Current browser smoke only confirms the game boots and renders after the spitter AI change. A dedicated obstacle/LOS scenario for visually proving the new reposition behavior is still a follow-up if we want stronger gameplay validation.
- 2026-04-23: Arena weapon merge pacing was accelerated for sword/gun base lines. Merging two Lv.4 base weapons now opens branch selection immediately, and the chosen branch jumps straight to Lv.8 / MAX instead of climbing through extra merges.
- 2026-04-23: Arena weapon sell value now follows tracked invested cost (`investedCost`) so the new Lv.4 -> MAX evolution path does not create a sell-profit exploit.
- 2026-04-23: `npm run lint` and `npm run build` passed after the arena weapon merge update. Local build smoke still boots successfully (`output/spitter-smoke-clean/shot-0.png`) with no fresh `errors-0.json`, but this smoke is still a general boot check rather than a targeted arena-merge gameplay script.
- 2026-04-25: Mobile shop UX pass started. Relaxed global `touch-action` from `none` to `auto` at the app root, then marked arena shop list regions with explicit `pan-y` / `pan-x` scroll containers so mobile stat lists, the weapon strip, and the shop grid can scroll more reliably without fighting the browser gesture system.
- 2026-04-25: Arena shop footer now adds mobile bottom safe-area padding so the reroll / next-wave buttons sit higher above mobile browser UI and are less likely to be clipped by the bottom bar.
- 2026-04-25: Owned-item display in the arena shop now stacks duplicate passives into a single chip with `xN`, and the chip row scrolls horizontally instead of wrapping into a tall block that covers the shop.
- 2026-04-25: `npm run lint` and `npm run build` both passed after the mobile shop changes.
- 2026-04-25: Playwright smoke could not complete in this environment. Sandbox launch failed with `spawn EPERM`, and an escalated retry still failed with Chromium `spawn UNKNOWN`, so browser screenshot validation remains pending for the mobile shop pass.
- 2026-04-25: Arena passive pool now removes `boxing_gloves` and `big_bullet`, adds unique `apple_tree`, and shop card generation filters out unique items once the player already owns them.
- 2026-04-25: Arena waves now spawn one non-colliding apple tree near each player who owns `apple_tree`; the tree persists for the whole wave, respawns at a new spot next wave, and drops one heal apple every 10 seconds.
- 2026-04-25: Apple pickups now use a dedicated `apple` item render, restore a fixed 15 HP on pickup, and trigger the existing heal VFX flow without adding collision geometry.
- 2026-04-25: Apple tree state is now serialized through `NetworkSyncSystem`, so online clients can see the tree prop and its drop cadence instead of only the synced apple items.
- 2026-04-25: `npm run lint`, `npm run build`, and build-smoke boot validation all passed after the apple tree update. Fresh smoke output was captured at `output/spitter-smoke-clean/shot-0.png` with no new `errors-0.json`.
- 2026-04-25: Arena shop card odds were retuned to skew heavily toward weapons. A 5-card shop now usually rolls around `1 passive / 4 weapons`, with a small variance band that can drift to `0` or `2` passives.
- 2026-04-25: Removed the `knife` passive from the arena passive pool and cleaned the last `critChance` color mapping residue from the arena shop stat UI.
- 2026-04-25: Apple tree tuning pass landed: tree visuals are larger, apple drops scatter across a much wider radius so they do not stack on one spot, and each apple now heals `50 HP`.
- 2026-04-25: Arena wave starts now recenter players at the middle of the arena before combat begins, preventing the next wave from inheriting a corner position from the previous round.
- 2026-04-25: `npm run lint`, `npm run build`, and build-smoke boot validation all passed after the arena shop ratio / apple tree / recenter pass. `output/spitter-smoke-clean/shot-0.png` was refreshed again with no new `errors-0.json`.
- 2026-04-25: Arena shop passive odds were tightened so a 5-card shop now always rolls at least 1 passive and at most 2 passives, while still favoring the `1 passive / 4 weapons` layout.
- 2026-04-25: Gun knockback pass completed. Projectile knockback is now a numeric strength, all base gun bullets spawn with `knockback: 1`, and the same baseline push now applies across bullet, missile, and arc gun paths while preserving the existing "big enemies shrug off low-level guns" behavior for the basic bullet path.
- 2026-04-25: `npm run lint`, `npm run build`, and build-smoke boot validation all passed after the gun knockback update. Fresh smoke output was captured again at `output/spitter-smoke-clean/shot-0.png` with no new `errors-0.json`.
- 2026-04-25: Visual-effects audit only. Reviewed current state reads for speed boost, slow debuff, shield, heal, and altar fire buff. Current player-body status rendering is very light: shield = single cyan ring, slow = single green ring, speed boost has no dedicated player aura, heal uses a floating `+` ring VFX, and altar buff currently reads mostly through the altar floor decal plus downstream fire/lava effects rather than a player-carried buff aura. Prepared a full art-upgrade plan for approval before implementation.
- 2026-04-25: Status VFX art pass implemented. `PlayerRenderer.ts` now gives speed boost a cyan wind-tail / streak aura, slow a sticky ooze puddle and drag strands, shield a layered blue energy shell with orbiting shards, regen a soft green body pulse, and altar buff a dedicated ember+rune aura on the player instead of only reading through downstream attacks.
- 2026-04-25: Heal VFX rebuilt in `EffectRenderer.ts` into three variants: `regen`, `aura`, and `burst`. Regen ticks now look lighter and more continuous, healing-aura ticks from streetlights look like soft upward pulses, and apple/pickup heals land as a bigger bloom.
- 2026-04-25: Added `isInvincible` to `Player` so an existing arena transition assignment in `Game.ts` is now typed correctly and matches the intended "boss death / transition safety" behavior.
- 2026-04-25: Verification after the VFX pass: `npm run build` passed, `npm run lint` passed, and a local build smoke run rendered successfully to `output/spitter-smoke-clean/shot-0.png`. This smoke is still a general gameplay render check rather than a targeted scripted capture of each status state.
- 2026-04-25: Follow-up VFX intensity pass landed. Fire altar buff is now hotter and more aggressive (`PlayerRenderer.ts` ground rune ring/glow + forward flame flare/sparks), speed boost is cleaner/brighter with bow-wave + longer cyan wake, slow debuff is darker/heavier with a wider sludge puddle and drag strands, and shield now reacts to blocked hits via `shieldHitFlashTimer` with a bright expanding impact pulse.
- 2026-04-25: `Player.ts` now tracks `shieldHitFlashTimer`; blocked damage refreshes a short 220ms hit reaction window and `update()` counts it down so the renderer can show a shield-impact flash without affecting gameplay logic.
- 2026-04-25: Verification after the intensity pass: `npm run build` passed and `npm run lint` passed again.
- 2026-04-25: Browser-smoke attempts for an in-game VFX screenshot are still blocked by the current start-screen automation flow. Standard build smoke still only captured the menu (`output/spitter-smoke-clean/shot-0.png`), selector/canvas hybrid attempts were saved under `output/status-vfx-smoke*`, and even a temporary force-click Playwright script (`output/status-vfx-force-smoke.cjs`) still remained on the start screen. No fresh browser console errors were emitted during these runs, but a dedicated menu-start automation fix is still needed for targeted gameplay screenshots.
- 2026-04-26: Healing color-system cleanup landed. The three heal VFX variants in `EffectRenderer.ts` (`regen`, `aura`, `burst`) now use named hex palettes plus 8-digit hex alpha values via `withHexAlpha(...)` instead of direct inline `rgba(...)` color strings, making the color choices easier to reason about and tune.
- 2026-04-26: Player-side `drawRegenAura()` in `PlayerRenderer.ts` was updated to the same hex-palette approach so the self-regen body aura matches the three heal VFX variants and no longer mixes direct `rgba(...)` values in that healing-specific renderer block.
- 2026-04-26: Verification after the healing hex-palette pass: `npm run build` passed, `npm run lint` passed, and build-smoke boot validation refreshed `output/spitter-smoke-clean/shot-0.png` with no fresh `errors-0.json`. This smoke still only confirms clean boot/render, not in-game healing-state capture.
- 2026-05-01: Player sprite integration started. Added generated 8x3 P1/P2 walking sheets under `public/sprites/players`, replaced the old circle body render with a chroma-keyed sprite-sheet draw path in `PlayerRenderer.ts`, and added `Player.isMoving` so idle uses the middle frame while movement cycles the 3 walking frames. `npm run build` passed; browser smoke / screenshot verification is next.
- 2026-05-03: Player 1 was switched to a recentered 8x3 walking sheet (`player1_walk_centered_8x3.png`) built from the generated blue survivor frames. Runtime magenta cleanup was widened to catch edge pixels, and P1 draw height was tuned separately for the fixed 128px cells. `npm run build` passed; escalated Playwright smoke reached gameplay and captured `output/player-sprite-game-smoke/gameplay.png` with no new `errors.json`, though the fullscreen hint overlay still dimmed the screenshot.
