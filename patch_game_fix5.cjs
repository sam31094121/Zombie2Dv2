const fs = require("fs"); let c = fs.readFileSync("src/game/Game.ts", "utf8"); c = c.replace(/  randomArenaPoint\(padding: number = 0\) \{/g, "  clampToArenaBounds(x: number, y: number, padding: number = 0) { const bounds = this.playableArenaBounds; return { x: Math.max(bounds.left + padding, Math.min(bounds.right - padding, x)), y: Math.max(bounds.top + padding, Math.min(bounds.bottom - padding, y)) }; }\n\n  randomArenaPoint(padding: number = 0) {"); fs.writeFileSync("src/game/Game.ts", c, "utf8");
